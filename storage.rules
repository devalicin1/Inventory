rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Development rules - allow all access for now
    // TODO: Implement proper authentication and restore security rules
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}

